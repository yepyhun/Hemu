---
context: phase
phase: 19-query-shape-conditioned-candidate-seeding
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-04-08T11:16:03.209Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Hard-38 truth is locked — The paid canonical replay stayed `3/38`; `retrieval_failure` remained `29/38`; `selector_engaged_cases = 0`; `aggregation_safety_abstentions = 0`. Any resumed work must start from this fact, not from local optimism.
- [ ] CONSTRAINT: Do not treat slice taxonomy as runtime ontology yet — Use slices only as diagnostic/experimental targeting and stop-rule language unless multiple phases prove they are stable.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Local-proof delusion | A mechanism looks clean locally, but the canonical hard bucket does not move at all. | blocking | Require explicit movement in `evidence_present_cases`, `sufficient_retrieval_cases`, or seed-opening traces on a frozen target subset before claiming engagement. |
| Premature ontology growth | Turning diagnostic slices into broad kernel truth objects too early recreates the old project's complexity trap. | advisory | Keep slice definitions in phase/reference artifacts first; only promote what survives multiple phases. |

<current_state>
We are paused in `Phase 19` mid-execute.

The phase was replanned after the paid hard `38` replay showed:
- `3/38`
- dominant live bucket still `retrieval_failure (29/38)`
- `selector_engaged_cases = 0`
- `aggregation_safety_abstentions = 0`

That means the current question is not how the selector should choose better, but whether a minimal query-shape-conditioned upstream seed-opening seam can make answer-bearing candidates appear at all.

Some code work has already started:
- `agent/core2_query_shape_seeding.py`
- `agent/core2_hybrid_substrate.py`
- `agent/core2_runtime.py`

But the phase is intentionally paused before final testing/proof because new external research may materially improve or alter the seam choice.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Freeze target subset and engagement criterion - Done at planning level
- Task 2: Replan Phase 19 against hard-38 truth - Done
- Task 3: Minimal upstream seed-opening mechanism - In progress, initial module and wiring exist but are not yet proven
</completed_work>

<remaining_work>

- Integrate the new external research into the seam choice
- Decide whether `query_shape_conditioned_candidate_seeding` remains the right bounded mechanism
- Add targeted tests for seed opening
- Prove boundedness and write missing execute artifacts:
  - `19-TARGET-SUBSET-MANIFEST.json`
  - `19-SEEDING-SEAM-RULES.md`
  - `19-SEEDING-BOUNDARY.md`
  - `19-SEEDING-TRANSITIONS.jsonl`
  - `19-OUTCOME.json`
  - `19-VERDICT.md`
  - `19-VERIFICATION.md`
</remaining_work>

<decisions_made>

- Keep the phase open; do not close it before new research arrives
- Treat the hard `38` replay as locked truth, not a soft suggestion
- Keep slice definitions as experimental taxonomy, not runtime ontology
- Allow only narrow upstream seeding borrow; no delivery reopen, no ranking reopen, no broad retrieval rewrite
</decisions_made>

<blockers>
- Human input pending: the user is gathering new jackpot research answers that may sharpen or replace the current seam hypothesis
</blockers>

## Required Reading (in order)
1. [19-EXECUTION-CHECKPOINT.md](/home/lauratom/Asztal/ai/hermes-agent-core2/.planning/phases/19-query-shape-conditioned-candidate-seeding/19-EXECUTION-CHECKPOINT.md) — frozen truth and what is still unproved
2. [19-RESEARCH.md](/home/lauratom/Asztal/ai/hermes-agent-core2/.planning/phases/19-query-shape-conditioned-candidate-seeding/19-RESEARCH.md) — phase seam taxonomy and allowed external borrow
3. [19-02-PLAN.md](/home/lauratom/Asztal/ai/hermes-agent-core2/.planning/phases/19-query-shape-conditioned-candidate-seeding/19-02-PLAN.md) — intended minimal mechanism
4. [19-HARD-38-ATTRIBUTION-STATUS.json](/home/lauratom/Asztal/ai/hermes-agent-core2/.planning/phases/19-query-shape-conditioned-candidate-seeding/19-HARD-38-ATTRIBUTION-STATUS.json) — canonical hard replay truth

## Critical Anti-Patterns (do NOT repeat these)
- Treating local route notes or pretty traces as proof of hard-bucket engagement → require hard subset movement
- Expanding Phase 19 into broad staged retrieval or context-tree adoption → keep it to one upstream seed-opening seam

## Infrastructure State
- No required background service is being intentionally kept alive for this pause
- Paid replay results already exist in Phase 19 artifacts
- Working tree contains uncommitted Phase 19 code edits

<context>
The key mental model is: the wall is earlier than the selector.

Phase 13/14 were not proven harmful; they simply did not engage the canonical hard bucket. Phase 19 must therefore avoid the old mistake of polishing the wrong downstream layer. The right question is whether a bounded query-shape-conditioned seed class can cause upstream raw/turn hits to appear on a homogeneous subset of the hard retrieval failures.
</context>

<next_action>
Start with: ingest the user's new jackpot research answers, compare them against the current Phase 19 seam, and decide whether to (a) refresh `/gsd-plan-phase 19` with the stronger seam framing or (b) resume `/gsd-execute-phase 19` and prove the current seed-opening mechanism with targeted tests.
</next_action>
