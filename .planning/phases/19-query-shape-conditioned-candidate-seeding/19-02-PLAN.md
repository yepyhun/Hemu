# Plan 19-02: Implement One Bounded Decomposition-And-Seeding Stack

## Goal

Implement one bounded query-shape-conditioned decomposition-and-seeding stack that can open the missing candidate classes on the frozen target subset before selector logic runs.

## Tasks

- implement one bounded upstream stack for the frozen subset:
  - operator/arity detection
  - slot extraction
  - bounded rewrite family
  - bounded seed families
- allowed seed families inside this phase:
  - clause-level rewrites
  - temporal anchor seeds
  - pairwise comparison seeds
  - aggregate constituent operand seeds
- add explicit route-note or trace markers that distinguish:
  - operator/slot family detected
  - seed opened
  - seed not applicable
  - seed blocked by earlier seam
- keep explicit exclusions for:
  - selector tuning
  - delivery work
  - ranking reopen
  - full staged retrieval adoption
  - full context-tree adoption
  - bitemporal/state refactor
  - aggregation-safety redesign
  - calibrated override gate work

## Deliverables

- bounded code change for one upstream decomposition-and-seeding family
- updated observability notes inside the runtime path
- `19-SEEDING-BOUNDARY.md`

## Verification

- the mechanism changes only the upstream decomposition-and-seeding seam
- selector logic remains untouched
- the exclusion list is explicit and testable
- the new trace markers are sufficient to tell whether the mechanism actually engaged
