# Phase 08.1 Invariants

## Imported Invariants

### INV-1

**Rule:** Covered-family authoritative answers must be backed by structured answer surfaces rather than raw-only fallback when a covered fact path exists.

**Core2 checker:** `check_structured_truth_precedence`

**Representative proof:** covered preference recall reaches `fact_first` authoritative path.

### INV-2

**Rule:** Deterministically resolvable current-state digest facts must not leave contradictory active canonical states for the same identity key.

**Core2 checker:** `check_current_state_uniqueness`

**Representative proof:** current residence update leaves one active state and one superseded prior state.

### INV-3

**Rule:** Obvious tool/file error artifacts must not remain active after explicit noise repair.

**Core2 checker:** `repair_core2_noise` + fixture-backed state assertions

**Representative proof:** synthetic tool/file artifacts are rejected, ordinary records are preserved.
