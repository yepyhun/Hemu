# Plan 06-03: Classify Comparison Result And Residual Risk

## Goal

Turn the baseline and hybrid `70`-sample artifacts into a single canonical comparison outcome that Phase 7 can consume without reinterpretation.

## Tasks

1. Read the normalized baseline and hybrid outcome artifacts from the frozen `70`-sample run and compute deltas as `hybrid - baseline`.
2. Apply the locked verdict precedence from Phase 5 to classify the result as `promote_candidate`, `keep_baseline`, or `mixed_hold`.
3. Record residual risks and any remaining judge-artifact or abstention caveats without opening a fix phase inside this milestone.
4. Write `06-COMPARISON-OUTCOME.json` as the single truth source for the Phase 7 default-selection decision.
5. Explicitly mark the exploratory `20`-sample raw status artifacts as superseded context rather than final Phase 6 evidence.

## Verification

- A single canonical comparison outcome artifact exists.
- The verdict matches the locked precedence and thresholds from Phase 5.
- The comparison artifact is sufficient for Phase 7 to make a bounded default-selection decision.
- The comparison artifact makes clear that the earlier exploratory `20`-sample raw files are superseded.
