# Plan 06-01: Run Shipped Baseline Comparison Proof

## Goal

Freeze the exact broader `70`-sample baseline comparison contract and execute the shipped v1.0 baseline under it.

## Tasks

1. Define the exact frozen `70`-sample question-id set and write it into a Phase 6 run manifest before any paid execution begins.
2. Use the locked Phase 6 run manifest to confirm the baseline runtime mode, sample, and artifact paths.
3. Run the locked baseline prerequisite proof command exactly as defined in the manifest.
4. Execute the broader paid baseline run against the frozen `70` question set with `CORE2_HYBRID_SUBSTRATE_MODE=off`.
5. Write the raw baseline status artifact and the normalized baseline outcome artifact using the Phase 5 schema.

## Verification

- A Phase 6 frozen `70`-sample manifest exists before the baseline paid run starts.
- The baseline run uses the frozen `70` question ids from the Phase 6 manifest.
- The raw and normalized baseline artifacts both exist.
- The normalized baseline artifact matches the locked schema fields.
