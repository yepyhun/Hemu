# Plan 04.8-01: Define Generalized Surface Contracts And Routing Boundaries

## Objective

Define the minimal reusable Core2 surface contracts that cover the three clustered gaps from the paid `20`-run without opening a new family explosion.

## Must Haves

- A generalized aggregate surface contract that covers totals and thresholds
- A generalized event-order / temporal-compare surface contract
- A canonical abstention surface contract for grounded insufficiency
- A fixed surface precedence rule across promptless surfaces, generalized surfaces, and fallback
- Explicit routing rules for when these surfaces are eligible
- Explicit anti-loop exclusions for what does **not** belong in this phase

## Tasks

1. Define the new generalized surface shapes and their required evidence/support fields.
2. Document the routing boundary between:
   - existing promptless authoritative surfaces
   - the new generalized non-promptless surfaces
   - fail-closed fallback
3. Fix the surface precedence order so execution cannot oscillate between overlapping routes.
4. Add a small failed-cluster-to-pattern mapping table that assigns each of the previously failed `20`-run cases to:
   - aggregate/threshold
   - event ordering
   - canonical abstention
   - explicit out-of-scope
5. Name the blocker classes this phase is meant to eliminate from the current failed cluster.
6. Record the explicit exclusions:
   - no topic-specific family rescue
   - no comparator broadening
   - no benchmark-string coupling

## Verification

- `04.8-CONTEXT.md` and `04.8-RESEARCH.md` make the phase boundary explicit and anti-loop.
- The plan names the three generic surfaces and the explicit exclusions.
- The precedence order is explicit enough that later execution cannot drift into route ambiguity.
- The failed-cluster mapping table makes it obvious if new one-off rescue work tries to sneak into the phase.
- The routing description is clear enough that later code changes can be judged against it mechanically.
