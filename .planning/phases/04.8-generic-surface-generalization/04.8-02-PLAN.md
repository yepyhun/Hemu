# Plan 04.8-02: Implement The Three Generic Surface Families

## Objective

Implement the smallest bounded Core2 changes that make aggregate totals/thresholds, event ordering, and canonical abstention surfaceable without topic-specific branching.

## Must Haves

- Generic aggregate total / threshold surfacing
- Generic short-list event ordering / temporal compare surfacing
- Canonical structured abstention surfacing
- Thin runtime wiring that prefers these surfaces before weak prompt reasoning
- No benchmark-specific text logic in the deterministic core

## Tasks

1. Extend existing surface/authoritative/runtime modules to emit the three generalized surface classes.
2. Reuse current fact/state objects where possible instead of inventing a parallel truth model.
3. Add the smallest routing logic needed so the relevant failed-cluster cases stop collapsing into generic prompt fallback.
4. Keep all changes inspectable through structured packets and machine-readable artifacts.
5. Preserve fail-closed behavior when the structured basis is insufficient.

## Verification

- New or adapted Core2 tests prove each surface class locally.
- No new topic-specific family registry explosion appears in the implementation.
- No comparator or judge logic is broadened as part of this plan.
