# Plan 18-02: Classify Upstream Candidate-Pool Miss Shapes

## Goal

Classify the frozen pre-selector subset into a bounded upstream taxonomy that explains why answer-bearing evidence never enters the pool.

## Tasks

- define stable classification rules for the working pre-selector taxonomy
- use the jackpot-derived upstream lenses (`operator_fit/slot_coverage`, `entity_binding_quality`, `temporal_completeness/numeric_executability`) only as classification aids
- produce one per-case transitions ledger over the subset
- summarize counts by upstream miss shape

## Deliverables

- `18-CANDIDATE-POOL-TAXONOMY.md`
- `18-CANDIDATE-POOL-TRANSITIONS.jsonl`

## Verification

- every subset case receives exactly one upstream taxonomy label
- the taxonomy stays upstream-of-selector and does not smuggle in delivery, ranking, or judge buckets
- counts reconcile to the subset manifest size
