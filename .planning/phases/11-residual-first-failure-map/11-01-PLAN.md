# Plan 11-01: Freeze Manifest And Schema

## Goal

Freeze the exact diagnostic input and the canonical schema before any classification work begins.

## Tasks

1. Record the frozen hard residual `38` manifest as the official diagnostic input.
2. Define the canonical observability schema for per-case classification.
3. Write the first-failure bucket definitions in a human-auditable rules file.

## Deliverables

- `11-RESIDUAL-MANIFEST.json`
- `11-FIRST-FAILURE-RULES.md`
- initial schema section for `11-CASE-TRANSITIONS.jsonl`

## Verification

- manifest size equals the intended frozen residual set
- schema fields are fixed before classification starts
- bucket definitions are mutually intelligible and narrow enough to avoid free-form storytelling

## Out of Scope

- no case classification yet
- no runtime edits
- no benchmark rerun
