# Phase 04.11 Protocol

## Frozen Run Contract

- Comparable set: exact frozen `20` question ids from the prior broader baseline
- Runtime mode: `CORE2_HYBRID_SUBSTRATE_MODE=on`
- Benchmark mode: `core2`
- Benchmark-fast: enabled
- Canonical outcome file: `04.11-STATUS.json`

## Local Prerequisite

Run the targeted non-regression proof before any paid evaluation:

`./.venv/bin/python -m pytest tests/agent/test_core2_hybrid_substrate.py tests/agent/test_core2_retrieval_routing.py tests/agent/test_core2_generic_surface_generalization.py tests/agent/test_core2_handmade_acceptance.py -q`

## Verdict Thresholds

### Go

- no kernel regressions
- `pass_rate >= 0.75`
- `answer_surface_hit_rate >= 0.65`

### No-Go

- any kernel regression
- or `pass_rate <= 0.60`

### Mixed

- everything else

## Verdict Precedence

1. regression
2. mixed
3. go

## Anti-Loop Rules

- Do not widen the hybrid seam inside 04.11.
- Do not move thresholds after seeing the paid result.
- Do not replace the frozen question set.
- Do not let any summary overrule the canonical `04.11-STATUS.json` artifact.
