---
wave: 1
depends_on: []
files_modified:
  - agent/core2_types.py
  - agent/core2_store.py
  - agent/core2_runtime.py
  - tests/agent/test_core2_plane_model.py
autonomous: true
requirements:
  - MODEL-01
  - MODEL-02
  - MODEL-03
  - MODEL-04
---

# Plan 02-01: Implement Plane Schemas And Provenance Segmentation

## Objective

Replace the current flat Core2 note model with an explicit multi-plane persistence model that separates evidence, canonical truth, derived propositions, retrieval indices, and delivery views.

## Must Haves

- Core2 persists plane-aware records rather than one undifferentiated note bucket
- `raw_archive` and `canonical_truth` are structurally distinct
- provenance, source segmentation, and canonical object identity are first-class persisted fields

## Tasks

<task>
<id>02-01-T1</id>
<title>Define Core2 plane/object/state types from the Phase 2 spec</title>
<read_first>
- plan7vegrehajt.md
- agent/core2_types.py
- agent/core2_runtime.py
- .planning/phases/02-memory-model-and-state-semantics/02-CONTEXT.md
</read_first>
<action>
Extend the Core2 typed model so it can represent:
- planes
- canonical object kinds
- source/provenance payloads
- state categories needed by later transition logic

Do not collapse `canonical_truth` and `derived_propositions` into one generic kind.
</action>
<acceptance_criteria>
- `agent/core2_types.py` contains explicit plane identifiers
- `agent/core2_types.py` distinguishes canonical truth from derived propositions
- source/provenance fields are representable in typed structures
</acceptance_criteria>
</task>

<task>
<id>02-01-T2</id>
<title>Refactor SQLite persistence into plane-aware schemas and migrations</title>
<read_first>
- agent/core2_store.py
- agent/core2_types.py
- plan7vegrehajt.md
</read_first>
<action>
Replace or evolve the flat Phase 1 storage layout into plane-aware tables or an equivalent normalized schema that can durably represent:
- `raw_archive`
- `canonical_truth`
- `derived_propositions`
- `retrieval_indices`
- `delivery_views`

Preserve local inspectability and Hermes-home awareness.
</action>
<acceptance_criteria>
- `agent/core2_store.py` no longer treats all durable Core2 memory as a single flat note table
- raw evidence/source data has a distinct durable representation
- canonical truth objects have stable IDs and source lineage fields
</acceptance_criteria>
</task>

<task>
<id>02-01-T3</id>
<title>Add active tests for plane separation and provenance persistence</title>
<read_first>
- tests/agent/test_core2_provider_foundation.py
- tests/agent/test_core2_legacy_adapted.py
- .planning/legacy-test-seeds/kernel-memory/test_kernel_memory_memory_objects.py
</read_first>
<action>
Create `tests/agent/test_core2_plane_model.py` with deterministic tests that prove:
- evidence/raw and canonical data are stored separately
- canonical objects keep provenance/source pointers
- derived records are marked as derived rather than canonical truth

Keep tests local and SQLite-backed.
</action>
<acceptance_criteria>
- `tests/agent/test_core2_plane_model.py` exists
- it asserts plane separation explicitly
- it asserts provenance/source persistence explicitly
</acceptance_criteria>
</task>

## Verification

- Inspect SQLite schema and confirm plane separation exists
- Prove canonical objects preserve source/provenance metadata
- Prove active tests can distinguish canonical versus derived records
