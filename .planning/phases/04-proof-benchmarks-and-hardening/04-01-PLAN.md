# Plan 04-01: Expand Tests And Regression Coverage Around Core2 Behavior

## Objective

Broaden Core2 verification from narrow phase-targeted tests to stronger regression and integration-sensitive coverage around the real provider/runtime boundary.

## Must Haves

- Core2 coverage extends beyond unit-like contract tests into provider-manager integration behavior.
- Existing Hermes provider lifecycle surfaces remain covered while Core2-specific assertions get stronger.
- Relation/graph-like linkage and edge-case correctness gain explicit regression protection.

## Tasks

<task>
<id>04-01-T1</id>
<title>Extend MemoryManager-level end-to-end coverage for Core2</title>
<read_first>
- tests/agent/test_memory_plugin_e2e.py
- tests/agent/test_core2_provider_foundation.py
- agent/memory_manager.py
- plugins/memory/core2/__init__.py
</read_first>
<action>
Create a Core2-oriented end-to-end test that exercises:
- provider registration through `MemoryManager`
- tool routing through the manager
- sync/prefetch behavior
- coexistence with builtin memory under the one-external-provider rule

Keep this on the real Hermes provider/runtime boundary rather than dropping to isolated store-only calls.
</action>
<acceptance_criteria>
- Core2 has at least one active `MemoryManager`-level E2E test
- the test proves init → tool call → sync/prefetch → shutdown on the real provider seam
- the test would fail on wiring regressions, not only store regressions
</acceptance_criteria>
</task>

<task>
<id>04-01-T2</id>
<title>Add broader Core2 regression tests for relation quality and edge cases</title>
<read_first>
- tests/agent/test_core2_retrieval_routing.py
- tests/agent/test_core2_abstention_delivery.py
- agent/core2_runtime.py
- agent/core2_store.py
</read_first>
<action>
Add regression coverage for scenarios that are likely to drift:
- relation/graph-linked retrieval staying coherent rather than isolated
- abstention and conflict behavior under noisier record sets
- supersession/current-state retrieval under more realistic mixed histories
- delivery-view correctness under larger retrieval sets
</action>
<acceptance_criteria>
- new tests cover at least one relation-quality scenario and one noisy-edge-case scenario
- graph-like linkage quality becomes an explicit regression target
- the expanded suite remains deterministic
</acceptance_criteria>
</task>

<task>
<id>04-01-T3</id>
<title>Define the broader local regression command set for Core2</title>
<read_first>
- pyproject.toml
- .planning/phases/03-retrieval-and-answer-contracts/03-VERIFICATION.md
- tests/agent/
</read_first>
<action>
Define and validate a broader local regression command set that Phase 4 and later release work can rely on. Keep the command set realistic for the current environment and call out environment-sensitive gates separately.
</action>
<acceptance_criteria>
- Phase 4 has a documented broader local regression command set
- it includes the targeted Core2 suite plus new E2E/regression coverage
- environment-sensitive gates are explicitly labeled instead of silently skipped
</acceptance_criteria>
</task>

## Verification

- Prove a MemoryManager-level Core2 E2E test exists and passes
- Prove the broader regression suite includes new relation/edge-case coverage
- Prove the documented local regression command set actually runs in the current environment
