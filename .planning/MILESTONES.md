# Project Milestones: Hermes Core2 Memory Kernel

## v1.0 Core2 Kernel Baseline (Shipped: 2026-04-07)

## v1.1 Hybrid Breakthrough Search (Shipped: 2026-04-07)

## v1.2 Authoritative Eligibility Bridge (Shipped: 2026-04-07)

**Delivered:** A source-grounded Core2 memory kernel for Hermes with deterministic answer/abstention contracts, paid-gate proof, and a bounded hybrid retrieval substrate branch validated beyond the initial baseline.

**Phases completed:** 1-4, 04.1-04.12 (48 plans total)

**Key accomplishments:**
- Shipped Core2 as a real Hermes memory provider with isolated `agent/core2_*` modules and provider lifecycle integration.
- Established explicit planes, namespace/trust rules, temporal/versioning semantics, typed answer contracts, and fail-closed abstention.
- Closed the initial paid Hermes-path `10/10` gate and then generalized the remaining broader-sample surface gaps without reopening family spam.
- Compared Core2 and MemPalace by layer, then implemented a bounded hybrid substrate seam with reversible `on|shadow|off` control.
- Improved the broader paid validation result from `12/20` to `18/20` on the hybrid branch.
- Closed the final handoff residual and explicitly classified the remaining abstention-only judge issue as a legitimate stop-point rather than another open-ended loop.

**Stats:**
- 16 phases, 48 plans
- milestone archived to `v1.0-ROADMAP.md` and `v1.0-REQUIREMENTS.md`
- no formal `v1.0-MILESTONE-AUDIT.md` was present at archive time

**What's next:** Start a fresh milestone with `/gsd-new-milestone` and decide whether the next work is post-v1 release/comparison, broader hybrid evaluation, or a new bounded architecture branch.

## v1.3 Retrieval Ranking Borrow (Shipped: 2026-04-07)

**Delivered:** A bounded retrieval-ranking borrow inside the hybrid candidate-ordering seam, with locally verified ordering behavior and no widening of truth ownership, comparator logic, or family coverage.

**Phases completed:** 9 (3 plans total)

**Key accomplishments:**
- Chose retrieval ranking as the next bounded forward mechanism after the bridge and noise-repair imports failed to move the hard residual set.
- Imported a small approved set of ranking signals from the legacy stack into Core2-native code.
- Kept the ranking borrow confined to the hybrid seam with clear out-of-scope exclusions.
- Verified the ranking borrow locally with targeted ranking tests and wider Core2 regression.

**What's next:** Start a fresh milestone with `/gsd-new-milestone` and decide whether to run a bounded residual replay against the `3/38` reference or pursue another tightly scoped follow-up.

## v1.4 Ranking Rollback And Postmortem (Shipped: 2026-04-08)

**Delivered:** A clean rollback of the failed retrieval-ranking borrow from the active hybrid path, plus a canonical postmortem from the hard residual replay regression.

**Phases completed:** 10 (3 plans total)

**Key accomplishments:**
- Rolled ranking back out of the active hybrid route after the hard `38` replay regressed from `3/38` to `2/38`.
- Preserved the bounded hybrid seam and earlier hardening imports instead of overreacting to the failed ranking bet.
- Recorded canonical regression facts and a postmortem that treats ranking borrow as a falsified breakthrough path.
- Carried forward exactly one bounded next-direction recommendation: `Covered-Family Prompt Delivery Bridge`.

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to pursue the next bounded mechanism.

## v1.5 Residual Bottleneck Falsification (Shipped: 2026-04-08)

**Delivered:** A diagnostic-only milestone that mapped the first true failure point on the frozen hard residual `38` set and falsified delivery as the dominant next build direction.

**Phases completed:** 11 (3 plans total)

**Key accomplishments:**
- Froze the residual `38` manifest and canonical classification rules before any new build work.
- Produced a per-case first-failure ledger instead of relying on aggregate benchmark symptoms alone.
- Showed that the residual set is locally retrieval-dominant (`33/38`) rather than delivery-dominant.
- Replaced the previous carry-forward delivery recommendation with one bounded retrieval-side recommendation: `Residual Retrieval Coverage Gap Map`.

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to pursue one bounded retrieval-side mechanism.

## v1.6 Residual Retrieval Coverage Gap Map (Shipped: 2026-04-08)

**Delivered:** A diagnostic retrieval-side coverage map for the frozen `33` retrieval-dominant hard residual cases, reducing the next forward bet to one bounded aggregate-temporal retrieval expansion.

**Phases completed:** 12 (3 plans total)

**Key accomplishments:**
- Froze the retrieval-dominant subset and classified every case into a narrow four-class taxonomy.
- Showed that the dominant residual shape is `weak_or_partial_evidence` (`15/33`), with `wrong_subset_selection` (`11/33`) as the second bucket.
- Distinguished true no-evidence abstention cases from unsupported open-ended recommendation-style prompts.
- Replaced the generic retrieval-side carry-forward with one narrower next recommendation: `Bounded Aggregate-Temporal Retrieval Coverage Expansion`.

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to turn the dominant retrieval gap into one bounded build.

## v1.7 Bounded Aggregate-Temporal Retrieval Coverage Expansion (Shipped: 2026-04-08)

**Delivered:** A bounded constituent-anchor retrieval expansion for the safe aggregate-temporal tranche, with local proof of route-shift and explicit exclusion of plain current-total aggregate shapes.

**Phases completed:** 13 (3 plans total)

**Key accomplishments:**
- Turned the verified `weak_or_partial_evidence` map into one real retrieval-side build instead of reopening multiple competing bets.
- Landed constituent-anchor expansion only for temporal delta, elapsed, days, ratio/percentage/average, and pairwise compare shapes.
- Added explicit runtime observability for the route-shift via `hybrid_constituent_expand`.
- Preserved a strict boundary by excluding plain current-total aggregate shapes after the first wider pass regressed.

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to choose the next bounded direction, including whether to promote the jackpot research on budgeted evidence selection and aggregation safety into an active plan.

## v1.8 Budgeted Evidence Selector And Aggregation Safety (Shipped: 2026-04-08)

**Delivered:** A bounded selector-layer improvement for the safe aggregate-temporal retrieval tranche, with explicit aggregation-safety abstention and selector-side observability.

**Phases completed:** 14 (3 plans total)

**Key accomplishments:**
- Promoted the strongest jackpot research into one bounded retrieval-side milestone instead of widening Phase 13 directly.
- Replaced plain itemwise session-anchor pickup with a budget-aware selector on the supported compositional shapes.
- Added explicit aggregation-safety abstention for incompatible numeric composition.
- Made selector use and safety-triggered abstention visible through dedicated trace fields and runtime notes.

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to choose the next bounded direction after the selector-and-safety tranche.

## v1.9 Pipeline Attribution Dashboard (Shipped: 2026-04-08)

**Delivered:** A bounded per-case diagnostics layer that separates retrieval, sufficiency, reasoning/delivery, and judge-like outcomes inside the existing benchmark plumbing.

**Phases completed:** 15 (3 plans total)

**Key accomplishments:**
- Froze one stable attribution schema and one machine-readable contract before implementation.
- Added reusable per-case attribution records to the current evaluation path instead of inventing a second trace universe.
- Reused route notes and bounded confidence fields so future reruns can attribute failures by layer.
- Produced one canonical local sample outcome and verified the contract without reopening retrieval, delivery, or truth-model work.

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to choose the next bounded build or diagnostics follow-up using the new attribution contract.

## v1.10 Attributed Hard Residual Replay (Shipped: 2026-04-08)

**Delivered:** One real frozen hard residual replay using the phase-15 attribution contract, plus a canonical attributed outcome that distinguishes raw passes from grounded passes.

**Phases completed:** 16 (3 plans total)

**Key accomplishments:**
- Replayed the canonical hard `38` on the current active path with both gate status and per-case attribution artifacts.
- Showed that the raw replay stayed at `3/38`, but only `1/38` counts as an attributed grounded pass.
- Identified `retrieval_failure` as the dominant actionable bucket on the hard residual set (`29/38`).
- Reduced the next forward choice to one bounded retrieval-side follow-up instead of multiple competing bets.

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to pursue the bounded `retrieval_selection_followup` direction.

## v1.11 Retrieval Selection Followup (Shipped: 2026-04-08)

**Delivered:** A bounded follow-up over the verified `retrieval_failure` bucket that proved the hard replay miss sits before selector engagement, not inside selector choice among useful candidates.

**Phases completed:** 17 (3 plans total)

**Key accomplishments:**
- Froze the `retrieval_failure` subset from the canonical hard replay and classified every case.
- Showed that the subset is homogeneous: `29/29` are `no_answer_bearing_selection`.
- Proved that the current selector layer never engaged on the verified hard replay failure bucket.
- Reduced the next forward choice to one bounded upstream direction: `pre_selector_candidate_pool_followup`.

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to pursue the bounded `pre_selector_candidate_pool_followup` direction.

## v1.12 Pre-Selector Candidate Pool Followup (Shipped: 2026-04-08)

**Delivered:** A bounded upstream-of-selector diagnostic milestone that reduced the verified hard residual miss shape to one dominant actionable family.

**Phases completed:** 18 (3 plans total)

**Key accomplishments:**
- Froze the canonical `29`-case `no_answer_bearing_selection` subset from `v1.11/17`.
- Classified every case into one upstream candidate-pool miss shape instead of reopening broad retrieval ideation.
- Showed that the top two atomic miss shapes collapse into one bounded actionable family: `query_shape_conditioned_candidate_seeding` (`21/29`).
- Reduced the next forward choice to one bounded upstream retrieval direction instead of selector retuning.

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to pursue the bounded `query_shape_conditioned_candidate_seeding` direction.

## v1.13 Query-Shape Conditioned Candidate Seeding (Shipped: 2026-04-08)

**Delivered:** A bounded upstream decomposition-and-seeding stack for the verified query-shape-conditioned candidate-opening family, with strong local proof but no paid hard-sample improvement.

**Phases completed:** 19 (3 plans total)

**Key accomplishments:**
- Replanned the milestone from a minimal seed trick into a bounded operator-slot decomposition plus rewrite-and-seed stack.
- Added explicit seed families for temporal, aggregate, current/previous, and pairwise hard-residual shapes while keeping selector, delivery, ranking, and state work frozen.
- Proved local seed opening on representative families and preserved broad Core2 regression safety.
- Ran a random paid hard-sample gate and got `0/5`, preventing an external improvement claim.

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to choose the next bounded direction after the failed random hard-sample gate.

## v1.14 Ten-Case Retrieval Opening Forensics And Primitive Borrow (Shipped: 2026-04-08)

**Delivered:** A bounded forensic-plus-borrow milestone over a fixed representative ten-case hard slice, with strong local proof but a negative external gate.

**Phases completed:** 20 (3 plans total)

**Key accomplishments:**
- froze a representative ten-case hard slice and kept it fixed through replay
- codemapped legacy Hermes helper primitives without reopening full legacy architecture
- landed one narrow Core2-native query-signal primitive borrow and proved it locally
- ran the fixed ten-case external gate to completion and recorded the negative verdict honestly

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to pursue a more upstream forensic milestone rather than another mid-pipeline heuristic borrow.

## v1.15 Fixed-Ten Memory Presence And Admission Forensics (Shipped: 2026-04-08)

**Delivered:** A fixed-ten forensic milestone that proved the dominant hard-slice seam is not source absence or gross persistence/index loss, but downstream post-recall answer-surface / handoff loss.

**Phases completed:** 21 (3 plans total)

**Key accomplishments:**
- Froze a reproducible lifecycle protocol for the same representative fixed `10`.
- Proved that all fixed-ten cases have answer-bearing dataset sessions and persisted canonical records in the mapped answer-session positions.
- Proved that all fixed-ten cases are searchable locally inside their answer-bearing sessions.
- Showed that direct Core2 recall reaches answer-bearing sessions in `9/10` cases.
- Reduced the dominant explanation from “memory is not there” to `post-recall surface / handoff loss`, with only one residual global opening miss.

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to pursue the bounded `Fixed-Ten Handoff And Answer-Surface Forensics` direction.

## v1.16 Fixed-Ten Handoff And Answer-Surface Forensics (Shipped: 2026-04-08)

**Delivered:** A verified downstream-forensic milestone that localized the fixed-ten hard-slice miss from broad post-recall loss to authoritative answer-surface / payload bridging.

**Phases completed:** 22 (3 plans total)

**Key accomplishments:**
- Froze a reproducible downstream protocol over the same fixed `10` hard cases.
- Traced packet, surface, payload, and handoff checkpoints instead of treating the downstream path as one black box.
- Showed that the dominant seam is missing promptless authoritative bridge coverage, not generic prompt assembly randomness.
- Proved that the few promptless-authoritative short-circuit cases currently present are still wrong, so payload correctness is part of the seam.
- Reduced the next bounded direction to `bounded_authoritative_surface_payload_expansion`.

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to pursue the bounded `bounded_authoritative_surface_payload_expansion` direction.

---

## v1.1 Hybrid Breakthrough Search (Shipped: 2026-04-07)

**Delivered:** A locked baseline-vs-hybrid comparison, an honest `mixed_hold` broader verdict (`31/70` baseline vs `32/70` hybrid), and one bounded breakthrough hypothesis with an anti-loop stop rule.

**Phases completed:** 5-7 (9 plans total)

**Key accomplishments:**
- Froze the comparison protocol before broader paid validation.
- Measured baseline and bounded hybrid on the same frozen `70`-sample set.
- Confirmed hybrid was directionally better without visible correctness regression, but not an automatic promotion winner.
- Reduced the next step to a single breakthrough candidate: authoritative-eligibility bridge for already-covered families.
- Explicitly forbade falling back into case-by-case benchmark patching.

**What's next:** Start a fresh milestone with `/gsd-new-milestone` and turn the authoritative-eligibility bridge into one bounded build phase.

---
