# v1.14 Ten-Case Retrieval Opening Forensics And Primitive Borrow

**Goal:** Inspect a representative fixed `10`-case hard-failure slice deeply, borrow one narrow primitive family only, and test whether that bounded borrow moves the replay.

## Completed Phases

### Phase 20: Ten-Case Retrieval Opening Forensics And Primitive Borrow

**Goal:** Freeze a representative `10`-case hard-failure slice, trace the exact upstream death seam, borrow only the narrow legacy/query-signal and temporal primitives that fit Core2, and test whether one bounded primitive-level import moves the replay.

**Status:** Verified negative result

**Delivered:**
- froze the fixed representative `10`-case hard slice
- codemapped old Hermes primitive helpers and kept the borrow boundary narrow
- implemented one Core2-native legacy query-signal primitive family
- proved the borrow locally with targeted tests and broad Core2 regression
- ran the fixed external `10`-case gate to completion

**External result:**
- `0/10`
- `retrieval_failure: 8`
- `sufficiency_failure: 1`
- `delivery_surface_failure: 1`
- `selector_engaged_cases = 0`

## Milestone Summary

**Delivered:** A bounded forensic-plus-borrow milestone that produced strong local proof but a negative external gate on the fixed representative ten-case slice.

**Phases completed:** 20 (3 plans total)

**Key accomplishments:**
- replaced small-sample intuition with a fixed representative ten-case forensic slice
- recorded a reusable case ledger, codemap, and borrow-boundary artifacts
- proved the narrow legacy query-signal primitive borrow integrates cleanly into Core2
- falsified that primitive family as an externally effective fix on the fixed ten-case gate

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to pursue a more upstream forensic direction, likely around memory presence, admission, indexing, or handoff loss on the fixed representative cases.
