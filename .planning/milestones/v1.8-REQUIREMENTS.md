# v1.8 Requirements Archive

Milestone: v1.8 Budgeted Evidence Selector And Aggregation Safety
Archived: 2026-04-08

## Completed Requirements

- `RETR-16` validated in `v1.8/14` — budgeted evidence selection now guides the bounded aggregate-temporal retrieval tranche instead of relying on itemwise pickup alone
- `REL-07` validated in `v1.8/14` — narrow aggregation safety now blocks incompatible numeric composition and partial-evidence overreach on the bounded selector path
- `OBS-03` validated in `v1.8/14` — selector-side observability now separates slot/sufficiency gains from aggregation-safety abstention

## Notes

This milestone stayed intentionally narrow. It did not reopen ranking, delivery, bitemporal remodeling, or promotion-gate calibration. It shipped one selector-layer improvement plus one bounded safety layer over the already-supported aggregate-temporal tranche.
