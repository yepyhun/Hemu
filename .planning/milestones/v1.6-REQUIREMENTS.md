# v1.6 Requirements Archive

Milestone: v1.6 Residual Retrieval Coverage Gap Map
Archived: 2026-04-08

## Completed Requirements

- `RETR-14` validated in `v1.6/12` — dominant retrieval-side gap shapes classified for the frozen hard residual `38` set
- `OBS-02` validated in `v1.6/12` — canonical retrieval coverage ledger recorded for the frozen `33`-case retrieval-dominant subset
- `FUT-17` validated in `v1.6/12` — the milestone ended with exactly one bounded retrieval-side build recommendation

## Notes

This milestone stayed diagnostic-only. It did not build a new retrieval mechanism; it narrowed the next build to one retrieval-side target and preserved a stop rule against reopening delivery, ranking, or multiple competing bets at once.
