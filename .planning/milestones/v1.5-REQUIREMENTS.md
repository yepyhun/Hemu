# v1.5 Requirements Archive

Milestone: v1.5 Residual Bottleneck Falsification
Archived: 2026-04-08

## Completed Requirements

- `OBS-01` validated in `v1.5/11` — canonical per-case first-failure map recorded for the frozen hard residual `38` set
- `QUAL-18` validated in `v1.5/11` — local replay separated retrieval-dominant cases from the small structured-route and delivery-like minority without reopening the benchmark loop
- `FUT-16` validated in `v1.5/11` — milestone ended with one bounded retrieval-side recommendation and an explicit stop rule against reopening the delivery bridge

## Notes

This milestone intentionally stayed diagnostic-only. It did not build a new mechanism. Its purpose was to falsify the wrong next bet before another implementation cycle began.
