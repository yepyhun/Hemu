# v1.13 Query-Shape Conditioned Candidate Seeding

## Goal

Turn the verified `query_shape_conditioned_candidate_seeding` carry-forward into one bounded upstream decomposition-and-seeding build without reopening broader retrieval redesign.

## Completed Phases

### Phase 19: Query-Shape Conditioned Candidate Seeding

**Delivered:**
- one bounded operator-slot decomposition layer for the target hard-residual slice
- one bounded rewrite-and-seed stack upstream of selector engagement
- explicit seed families for temporal, aggregate, current/previous, and pairwise shapes
- one local proof bundle and one replay-worthiness verdict without claiming external benchmark movement

**Key facts:**
- targeted local proof: `14 passed`
- broader Core2 regression: `150 passed`
- representative local seed opening proven for:
  - `temporal_elapsed`
  - `aggregate_numeric`
- schema detection proven for:
  - `temporal_elapsed`
  - `aggregate_numeric`
  - `current_previous`
  - `pairwise_compare`
- canonical phase verdict: `go_local_replay_worthy`
- random paid hard-sample gate after verify: `0/5`

## Milestone Summary

`v1.13` did not deliver an external breakthrough. It delivered a larger bounded upstream decomposition-and-seeding stack with strong local proof, but the follow-up random paid hard sample stayed at `0/5`, so this milestone does not justify claiming that the hard residual wall moved.

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to choose the next bounded direction after the failed random hard-sample gate.
