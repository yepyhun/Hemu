# Plan 22-01: Freeze The Exact Downstream Forensic Protocol

## Goal

Freeze an unambiguous downstream checkpoint protocol over the fixed ten so every later claim is tied to the same ordered chain from direct recall packet to final answer surface.

## Tasks

- reuse the fixed-ten manifest from `v1.14/20` and `v1.15/21`
- define the ordered downstream checkpoints from packet creation through final response
- define allowed downstream seam labels and their decision rules
- define what evidence must be captured at each checkpoint so the phase cannot degrade into black-box summary language

## Deliverables

- `22-DOWNSTREAM-PROTOCOL.md`
- `22-DOWNSTREAM-LABELS.md`
- `22-FIXED-TEN-MANIFEST.json`

## Verification

- the protocol uses the same fixed ten as `v1.14/20`
- the checkpoints are ordered and concrete
- the labels distinguish packet, surface, payload, prompt, and final-response loss
- the protocol prevents drift into speculative fix work before localization is complete
