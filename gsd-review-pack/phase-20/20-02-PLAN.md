# Plan 20-02: Codemap Narrow Primitives And Borrow One

## Goal

Codemap the narrow borrowable legacy and external primitives, then implement at most one primitive family that plausibly improves the frozen `10`-case upstream opening seam.

## Tasks

- codemap the old Hermes helper layer, explicitly distinguishing:
  - borrowable primitives
  - complexity traps
- required legacy codemap targets:
  - `kernel_memory_projection_signals.py`
  - `kernel_memory_objective_units.py`
- allowed external contrast:
  - MemPalace only as admission-loss / representation-loss inspiration
- choose at most one primitive family, for example:
  - query-hint / alias primitives
  - current/temporal signal primitives
  - unit-scope shaping primitives
- implement the chosen primitive family in isolated Core2-native form
- keep wiring thin and explicit
- document everything explicitly excluded:
  - whole legacy import
  - whole MemPalace-style adoption
  - selector retuning
  - delivery reopen
  - ranking reopen
  - broad retrieval redesign

## Deliverables

- `20-LEGACY-CODEMAP.md`
- `20-BORROW-BOUNDARY.md`
- one bounded Core2-native primitive borrow

## Verification

- only one primitive family is imported
- the imported logic is helper-level, not architecture-level
- the borrow remains isolated and reversible
- no forbidden layer is reopened
