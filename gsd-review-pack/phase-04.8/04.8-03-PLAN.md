# Plan 04.8-03: Prove The Failed-Cluster Closure And Set The Stop Rule

## Objective

Prove that the new generalized surfaces actually cover the previously failed `20`-run cluster and define the exact rerun/handback rule before MemPalace work begins.

## Must Haves

- Targeted proof for the previously failed aggregate/order/abstention cluster
- Handmade and golden-path proof still green
- A tiny negative-proof mini-set for lookalike cases that must still fail closed
- One authoritative machine-readable status artifact for the `04.8` phase path
- Explicit paid-rerun trigger rule
- Explicit stop rule if the cluster does not collapse cleanly

## Tasks

1. Build or extend targeted tests around the previously failed `20`-run cluster as exemplars of the three generic surface patterns.
2. Add a tiny negative-proof mini-set for near-miss cases that should still route to fail-closed fallback.
3. Record a machine-readable local status artifact showing which failed-cluster cases now resolve, abstain canonically, remain unresolved, or are explicitly out of scope.
4. Re-run handmade acceptance and relevant golden-path traces.
5. Define the next-step decision rule:
   - if the failed cluster is green locally, the next broader paid rerun is justified
   - if not, do not expand families again; hand off to the later substrate/adoption analysis

## Verification

- Local proof shows whether the previously failed cluster is now structurally covered.
- Negative proof shows that generalization did not quietly turn into over-broad matching.
- The paid rerun trigger is explicit and mechanical, not narrative.
- The phase ends with a clean handback either to a paid rerun or to the deferred MemPalace adoption phases.
