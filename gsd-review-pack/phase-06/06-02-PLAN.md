# Plan 06-02: Run Bounded Hybrid Comparison Proof

## Goal

Execute the bounded hybrid candidate under the exact same frozen `70`-sample contract and record its canonical hybrid artifacts for direct comparison.

## Tasks

1. Use the locked Phase 6 run manifest to confirm the hybrid runtime mode, sample, and artifact paths.
2. Run the locked hybrid prerequisite proof command exactly as defined in the manifest.
3. Execute the broader paid hybrid run against the same frozen `70` question set with `CORE2_HYBRID_SUBSTRATE_MODE=on`.
4. Write the raw hybrid status artifact and the normalized hybrid outcome artifact using the Phase 5 schema.

## Verification

- The hybrid run uses the same frozen `70` question ids as the baseline run.
- The raw and normalized hybrid artifacts both exist.
- The normalized hybrid artifact matches the locked schema fields.
