# v1.14 Requirements Archive

## Completed Requirements

- `RETR-20` — the fixed representative `10`-case hard slice was frozen and traced deeply enough to confirm the bottleneck remains upstream retrieval opening
- `QUAL-24` — only one narrow primitive family was borrowed, and no whole legacy or MemPalace-style architecture was imported
- `FUT-24` — the milestone ended with one canonical verdict on the fixed `10`-case replay: the bounded primitive borrow did not move it

## Notes

- This milestone is archived as a verified negative result, not as a benchmark-improvement milestone.
- The external acceptance gate is the authoritative verdict: strong local proof did not translate into replay movement.
