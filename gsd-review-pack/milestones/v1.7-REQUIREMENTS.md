# v1.7 Requirements Archive

Milestone: v1.7 Bounded Aggregate-Temporal Retrieval Coverage Expansion
Archived: 2026-04-08

## Completed Requirements

- `RETR-15` validated in `v1.7/13` — retrieval coverage now expands for the safe aggregate-temporal tranche without reopening ranking or delivery
- `QUAL-19` validated in `v1.7/13` — the bounded retrieval expansion was proven locally against the mapped target bucket with excluded shapes kept closed
- `FUT-18` validated in `v1.7/13` — the milestone ended with one canonical verdict: aggregate-temporal coverage was the right bounded next build

## Notes

This milestone shipped a bounded retrieval-side mechanism, not a full retrieval redesign. It explicitly kept plain current-total aggregate shapes out of scope after the first broader pass regressed, preserving a narrow seam for the next milestone instead of widening prematurely.
