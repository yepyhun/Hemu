# v1.10 Attributed Hard Residual Replay

## Goal

Apply the phase-15 attribution contract to one real frozen hard residual replay so the next build choice comes from attributed evidence instead of mixed raw benchmark symptoms.

## Completed Phases

### Phase 16: Attributed Hard Residual Replay

**Delivered:**
- one frozen replay manifest for the canonical hard `38`
- one standard gate status artifact plus one per-case attributed status artifact
- one canonical attributed replay outcome over the hard residual set
- one bounded next-direction recommendation from the replay rather than competing hypotheses

**Key facts:**
- raw replay result: `3/38`
- attributed grounded passes: `1/38`
- judge-false-positive passes inside the raw replay result: `2`
- dominant actionable bucket: `retrieval_failure` / `retrieval` with `29/38`

## Milestone Summary

`v1.10` did not deliver a score breakthrough. It delivered a harder and more useful truth: the current active path still fails primarily upstream in retrieval on the frozen hard residual set, and raw replay passes can overstate grounded progress when judge-false-positive cases are present.

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to pursue the bounded `retrieval_selection_followup` direction.
