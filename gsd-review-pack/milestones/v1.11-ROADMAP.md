# v1.11 Retrieval Selection Followup

## Goal

Follow up the verified `retrieval_failure` bucket from the attributed hard residual replay and reduce the retrieval-side next move to one bounded selection-focused direction.

## Completed Phases

### Phase 17: Retrieval Selection Followup

**Delivered:**
- one frozen selection subset manifest for the canonical `retrieval_failure` bucket
- one bounded selection taxonomy over that subset
- one per-case selection ledger
- one canonical carry-forward recommendation from the selection outcome

**Key facts:**
- subset size: `29`
- dominant selection shape: `no_answer_bearing_selection` (`29/29`)
- selector engaged cases: `0`
- carry-forward recommendation: `pre_selector_candidate_pool_followup`

## Milestone Summary

`v1.11` did not open another broad retrieval theory. It proved something narrower and more useful: on the verified hard replay failure bucket, the active path never even surfaces answer-bearing evidence, so the next move must be upstream of selector engagement.

**What's next:** Start a new milestone with `/gsd-new-milestone` when ready to pursue the bounded `pre_selector_candidate_pool_followup` direction.
