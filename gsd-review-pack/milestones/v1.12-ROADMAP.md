# Milestone v1.12: Pre-Selector Candidate Pool Followup

**Status:** SHIPPED 2026-04-08
**Phases:** 18-18
**Total Plans:** 3

## Overview

A bounded upstream-of-selector diagnostic milestone that reduced the verified hard residual miss shape to one dominant actionable family instead of generic selector tuning.

## Phases

### Phase 18: Pre-Selector Candidate Pool Followup

**Goal**: map the candidate-pool miss shape inside the frozen `29`-case retrieval subset and reduce the next build to one bounded upstream retrieval mechanism  
**Depends on**: `v1.11/17 Retrieval Selection Followup`  
**Plans**: 3 plans

Plans:

- [x] `18-01` Freeze Pre-Selector Candidate-Pool Subset
- [x] `18-02` Classify Upstream Candidate-Pool Miss Shapes
- [x] `18-03` Reduce To One Bounded Upstream Mechanism

**Details:**

- The phase froze the canonical `29`-case `no_answer_bearing_selection` subset from `v1.11/17`.
- The phase classified every case into one upstream taxonomy label without reopening delivery, ranking, or broad retrieval redesign.
- The top two atomic miss shapes were:
  - `temporal_anchor_seed_miss`: `11`
  - `query_shape_not_opened`: `10`
- Those two labels collapsed into one dominant actionable family:
  - `query_shape_conditioned_candidate_seeding`: `21/29`

## Milestone Summary

**Key Decisions:**

- Keep the milestone strictly upstream-of-selector.
- Do not reopen delivery, ranking, bitemporal, or promotion-gate work.
- Treat the top two atomic miss shapes as one bounded actionable family only if they truly share the same upstream seam.

**Issues Resolved:**

- Resolved whether the next move should be generic selector retuning. It should not.
- Reduced the hard residual carry-forward to one bounded upstream family.

**Issues Deferred:**

- `entity_scope_binding_followup`
- `stop_rule_or_non_authoritative_path`

**Technical Debt Incurred:**

- None intentionally; the phase stayed diagnostic and did not widen the runtime surface.

_For current project status, see .planning/ROADMAP.md_
