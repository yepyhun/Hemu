# Plan 04.5-03: Add Gate Matrix And Hand Back To 04.1

## Objective

Make the remaining live-gate state mechanically classifiable and cheap to resume so Phase 04.1 can rerun the real external gate on top of an explicit, deterministic handoff contract.

## Must Haves

- The gate artifact classifies failures into stable blocker families.
- Local proof shows that the answer-surface path is actually active.
- 04.5 ends by handing control back to the unfinished 04.1 live rerun, not by pretending the paid gate already happened.

## Tasks

- Extend the machine-readable gate artifact so it can record:
  - answer-surface hit rate or equivalent coverage signal
  - failure families such as `kernel_correctness`, `handoff_format`, `latency`, and explicit `unknown`
  - current blocker and rerun readiness
- Add a fail-closed rule for classification so ambiguous cases remain explicit instead of being forced into a cleaner category.
- Add a tiny frozen canary replay set from the existing real gate failures and make it part of the cheap pre-paid rerun proof path.
- Add focused tests and smoke proofs for the new gate classification and answer-surface path.
- Update any benchmark-adjacent proof helper needed to preserve the new failure taxonomy without hiding ambiguity behind free-text summaries.
- Update planning/proof artifacts so the next move after 04.5 is narrow and explicit:
  - resume 04.1
  - rerun the staged live gate
  - decide whether the final paid sample is justified

## Verification

- Tests prove that the gate artifact classifies failures mechanically.
- Tests prove that ambiguous failures stay ambiguous instead of being silently mislabeled.
- Tests prove that the frozen canary set runs through the intended cheap pre-gate path.
- Proof output can distinguish answer-handoff failures from kernel-correctness failures.
- 04.5 artifacts leave 04.1 with a clear rerun target instead of another vague “try again” loop.
