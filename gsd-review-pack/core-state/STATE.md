---
milestone: "v1.17"
milestone_name: Bounded Authoritative Surface Payload Expansion
current_phase: "23"
current_phase_name: Bounded Authoritative Surface Payload Expansion
current_plan: none
status: ready_for_verify
stopped_at: Phase 23 execute complete; fixed-ten paid gate finished at 1/10 with handoff-format still dominant
last_updated: "2026-04-08T22:36:00.000Z"
last_activity: 2026-04-08 — Executed v1.17/23 through the fixed hard ten; the bounded downstream bridge improved the slice to 1/10 but still needs work
progress:
  phases_total: 1
  phases_completed: 0
  plans_total: 3
  plans_completed: 3
---

# Project State

## Project Reference

- Project: Hermes Core2 Memory Kernel
- Current milestone: `v1.17 Bounded Authoritative Surface Payload Expansion`
- Current phase: `23 Bounded Authoritative Surface Payload Expansion`

## Current Position

- Current focus: verify Phase 23 against the finished fixed-ten external truth
- Next step: `/gsd-verify-work 23`
- Status: execute complete; fixed-ten result improved to 1/10 but still needs work

## Accumulated Context

### Decisions

- `v1.0` remains the archived shipped baseline and reference point
- `v1.1` established that hybrid is the forward path, but not by enough to count as automatic promotion on broader comparison alone
- `v1.2` completed its bounded bridge-and-hardening scope without reopening broad benchmark loops
- `v1.3` selects retrieval-ranking borrow as the next bounded forward direction
- `v1.3` completed its bounded ranking-borrow scope and left any broader replay or next mechanism choice to a fresh milestone
- The fresh paid residual replay falsified ranking borrow as a breakthrough path on the hard `38` set (`3/38 -> 2/38`)
- `v1.4` starts with rollback and postmortem instead of compounding the failed ranking path
- `v1.4/10` removed ranking from the active hybrid path and carried forward one bounded next-direction recommendation: `Covered-Family Prompt Delivery Bridge`
- `v1.5` starts by testing that carry-forward recommendation against a stricter diagnostic requirement: per-case first-failure evidence must justify any next build
- `v1.5/11` falsified delivery as the dominant next build direction; the hard residual `38` is locally retrieval-dominant (`33/38`)
- `v1.5/11` carried forward one new bounded retrieval-side recommendation: `Residual Retrieval Coverage Gap Map`
- `v1.6` started by turning that retrieval-side recommendation into one bounded gap-mapping milestone
- `v1.6/12` completed and verified a concrete retrieval coverage map with one carry-forward next direction: `Bounded Aggregate-Temporal Retrieval Coverage Expansion`
- `v1.7` starts by turning that carry-forward recommendation into one bounded build milestone
- `v1.14/20` executed a representative ten-case forensic-plus-borrow test with a narrow legacy query-signal primitive family; local proof stayed strong but the fixed external ten-case gate remained `0/10`
- `v1.14` is now archived as a verified negative-result milestone: the contract was met, but the borrowed primitive family did not improve the fixed ten-case external gate
- `v1.15` starts by moving one layer deeper than retrieval heuristics: proving memory presence, admission, indexing, and handoff loss on the fixed hard slice before any new build direction is promoted
- `v1.15/21` falsified source absence, gross ingest/persist loss, and session-local unsearchability on the fixed ten; the dominant seam is now post-recall surface/handoff, with only one residual global opening miss
- `v1.15/21` is now verified; the canonical carry-forward recommendation is `fixed_ten_handoff_surface_forensics`
- `v1.15` is now archived; there is no active milestone and the next bounded direction should start from the downstream handoff/surface seam
- `v1.16` starts by turning that carry-forward into one bounded downstream forensic milestone over the fixed ten, without reopening upstream retrieval heuristics first
- `v1.8` starts by promoting the strongest jackpot research into one bounded selector-and-safety milestone rather than widening Phase 13 directly
- `v1.10/16` completed one real attributed replay over the frozen hard `38`; the raw result remained `3/38`, but the dominant actionable bucket is now explicitly `retrieval_failure` (`29/38`)
- `v1.10/16` is now verified; the hard replay reduced the next forward choice to one bounded retrieval-side follow-up rather than multiple competing bets
- `v1.10` is now archived; the active carry-forward recommendation is `retrieval_selection_followup`
- `v1.11` starts by turning that carry-forward into one bounded retrieval-selection milestone instead of reopening broader retrieval expansion
- `v1.11/17` showed that the current hard replay selection miss is homogeneous: the active path never surfaces answer-bearing evidence on the `retrieval_failure` subset, so the next move is upstream of selector engagement
- `v1.11/17` is now verified; the canonical carry-forward recommendation is `pre_selector_candidate_pool_followup`
- `v1.11` is now archived; the active carry-forward recommendation is `pre_selector_candidate_pool_followup`
- `v1.12` starts by turning that carry-forward into one bounded upstream retrieval milestone rather than reopening selector tuning or delivery work
- `v1.12/18` is now verified; the canonical carry-forward recommendation is `query_shape_conditioned_candidate_seeding`
- `v1.12` is now archived; the active carry-forward recommendation is `query_shape_conditioned_candidate_seeding`
- `v1.13` starts by turning that carry-forward into one bounded upstream seeding build instead of widening into full staged retrieval or context-tree adoption
- `v1.13/19` was replanned after external research: the phase now allows one bounded operator-slot decomposition layer plus bounded rewrite/seed families, but still forbids full staged retrieval, state-semantics refactors, aggregation-safety redesign, or override-gate work

### Pending Todos

- Verify Phase `23` with the honest external gate result and bounded-scope framing

### Blockers/Concerns

- The hard residual replay showed ranking borrow did not help the active path enough to justify keeping it
- The delivery recommendation was falsified locally as the dominant next build; the next step must not drift back into delivery by inertia alone
- The next milestone must not reopen multiple retrieval hypotheses at once just because delivery was ruled out
- The next milestone must use the new attribution contract on a real replay before promoting another kernel-build hypothesis
- The raw `3/38` replay result overstates grounded success: only `1/38` is an attributed grounded pass, while `2` raw passes are judge-false-positive cases
- The next milestone should avoid reopening delivery or ranking from inertia; the verified replay still points upstream to retrieval
- The next milestone must stay bounded to one retrieval-side follow-up rather than broadening into multiple jackpot threads at once
- The new milestone must stay focused on selection miss shapes inside the verified `retrieval_failure` bucket, not general retrieval ideation
- The current phase result says the next bounded move must be pre-selector candidate-pool work, not another selector retuning loop
- The next milestone should stay upstream-of-selector and must not slide back into selector tuning by inertia
- The current phase may use one bounded operator-slot decomposition layer, but must not widen into full retrieval-stack redesign
- The new milestone may borrow staged admission ideas, but only as a narrow seeding pattern; it must not become a platform-style retrieval overhaul
- The paid hard replay after milestone start still stayed at `3/38`; phase 19 execution must treat that unchanged external truth as a gate, not as a reason to silently broaden scope
- The next milestone should not reopen presence/admission/index heuristics by inertia; Phase 21 showed the fixed ten is mostly failing downstream of direct recall
- The next milestone should start from `bounded_authoritative_surface_payload_expansion`, not from another broad retrieval or architecture detour
- The next milestone must prove downstream improvement on the same fixed-ten external gate before any stronger claim is made

## Session Continuity

If resuming later:
- read `PROJECT.md`, `ROADMAP.md`, `REQUIREMENTS.md`
- then read `23-BRIDGE-RULES.md`, `23-LOCAL-OUTCOME.json`, `23-VERDICT.md`, and the two `23-FIXED-TEN-*.json` gate artifacts
- then verify `Phase 23` against the finished `1/10` fixed-ten result
