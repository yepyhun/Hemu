# Plan 16-02: Run Bounded Attributed Replay

## Goal

Run one attributed replay over the frozen hard residual set using the current active hybrid path.

## Tasks

- execute the hard `38` replay with the current active path
- emit per-case attribution rows in the phase-15 contract format
- emit one attributed status artifact that summarizes the replay without losing the per-case detail
- keep the run bounded to the single frozen manifest

## Deliverables

- attributed replay execution output
- `16-ATTRIBUTED-STATUS.json`

## Verification

- replay completes against the frozen manifest
- per-case attribution rows exist
- attributed status summarizes the replay cleanly
- no kernel changes are made as part of the replay

## Guardrails

- no retrieval edits
- no delivery edits
- no benchmark threshold changes
- no second replay on a different subset inside the same phase
