# Phase 16-01 Summary

Froze the hard replay scope from the canonical `08.1` residual status artifact.

- the hard `38` question IDs are now explicit in `16-REPLAY-MANIFEST.json`
- the replay is locked to the current active path, not a historical ranking branch
- the phase reuses the `v1.9/15` attribution contract instead of inventing a second diagnostics schema
