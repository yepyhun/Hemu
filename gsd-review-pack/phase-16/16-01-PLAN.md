# Plan 16-01: Freeze Hard Replay Scope And Contract

## Goal

Freeze the hard residual replay scope and the exact attributed output contract before running the replay.

## Tasks

- define the canonical source artifact for the frozen hard `38` question IDs
- write the replay manifest with the exact question IDs and replay mode
- lock which phase-15 attribution fields are mandatory in the replay output
- define the bounded status/outcome artifact shape for this phase

## Deliverables

- `16-REPLAY-MANIFEST.json`
- schema notes embedded in phase artifacts

## Verification

- hard `38` question IDs are frozen explicitly
- the current active path is named explicitly as the replay target
- the attributed output contract is explicit before execution
- no new replay subset or alternate baseline is smuggled in
