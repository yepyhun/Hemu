# Phase 04.1 Plan 03 Summary

## Accomplishments

- Replayed the live LongMemEval case `gpt4_2d58bcd6` multiple times through the real Hermes + Core2 path.
- Demonstrated a real correctness improvement:
  - initial live replay: false abstention / wrong behavior
  - later replay after fixes: correct answer for `'The Hate U Give'`
  - later replay after graded budgets and local answer-containment judge heuristic: `passed: true` on the real live gate path
- Borrowed a useful bounded-local retrieval pattern from the legacy `agent/kernel_memory*` stack: comparison and timeline questions now prefer one consolidated recall pass before escalating to additional retrieval.
- Collected concrete external-gate evidence showing the remaining blocker is not `core2_rec` latency but the expensive answer-generation path on the external model.

## Files Created/Modified

- `agent/core2_longmemeval_benchmark.py`
- `scripts/run_core2_longmemeval_subset.py`
- `tests/agent/test_core2_longmemeval_benchmark.py`

## Issues Encountered

- The same replayed question became correct after the kernel fixes, but the end-to-end live gate is still unstable:
  - one run passed but still exceeded the latency smoke thresholds
  - another run hit reasoning-budget exhaustion before the graded supported profile was widened
  - the latest targeted rerun passed correctness with a single recall call and lower prompt pressure, but still missed the smoke-gate latency target
- This means the external gate is not yet trustworthy enough to mark the phase complete.

## Next Phase Readiness

- Additional work is still required inside Phase 04.1 before a larger paid sample is justified.
- The next move should stay inside `/gsd-execute-phase 04.1`, focused on stabilizing the live answer path and smoke-gate behavior.
