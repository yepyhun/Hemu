# Phase 04.1 Verification Report

## Goal Achievement

### Observable Truths

- The benchmark runner is now diagnosable and replayable by `question_id`.
- The replayed LongMemEval case `gpt4_2d58bcd6` exposed and then validated several real fixes:
  - personal-memory seeding namespace
  - lexical search precision
  - personal reading-history routing
  - graded tool-budget tiers
  - deterministic answer-containment judge shortcut for obvious hits
- A legacy `kernel_memory` pattern also helped: compare/timeline questions now prefer one bounded consolidated recall before a second retrieval pass.
- The same real live case that initially failed with false abstention later produced a `passed: true` result through the real Hermes path.
- The external gate is still not stable enough to claim success because smoke-gate latency remains above the configured threshold even after correctness, judge, and bounded-retrieval fixes.

### Required Artifacts

- `agent/core2_longmemeval_benchmark.py`
- `scripts/run_core2_longmemeval_subset.py`
- `agent/core2_store.py`
- `agent/core2_routing.py`
- `agent/core2_types.py`
- `plugins/memory/core2/__init__.py`
- `tests/agent/test_core2_longmemeval_benchmark.py`

## Verification Commands

- `.venv/bin/python -m py_compile agent/core2_longmemeval_benchmark.py agent/core2_store.py agent/core2_routing.py agent/core2_types.py plugins/memory/core2/__init__.py scripts/run_core2_longmemeval_subset.py tests/agent/test_core2_longmemeval_benchmark.py`
- `.venv/bin/python -m pytest tests/agent/test_core2_longmemeval_benchmark.py tests/agent/test_core2_retrieval_routing.py tests/agent/test_core2_provider_foundation.py tests/agent/test_core2_hardening.py -n 0`
- Live replay:
  - `scripts/run_core2_longmemeval_subset.py --mode core2 --question-id gpt4_2d58bcd6 --sample-size 1 --seed 7 --max-conversation-seconds 12 --max-total-wall-seconds 20`

## Verification Results

- Local targeted regression suite: passed
- Live replay correctness on `gpt4_2d58bcd6`: improved from false abstention to a real `passed: true` result
- Live smoke-gate latency: still failed, but the latest replay improved to one recall call, ~3818 prompt tokens, ~19.0s conversation time, and ~25.3s total wall time

## Gaps Summary

- Phase 04.1 is not complete yet.
- `QUAL-04` remains open because the staged paid gate is still unstable on the live Hermes path.

## Warnings

- Current evidence says the kernel correctness is materially better than before, but the external gate remains blocked by model-path latency and instability.
- Current evidence says the kernel correctness is materially better than before, and the judge no longer hides obvious hits behind an extra model call, but the external gate remains blocked by answer-generation latency.
- Current evidence says the legacy bounded-local retrieval pattern improved the live path materially, but not enough to trust a larger paid sample yet.
- A larger paid sample should not be trusted yet.
