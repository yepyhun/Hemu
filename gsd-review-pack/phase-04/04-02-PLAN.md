# Plan 04-02: Build End-To-End Proof/Benchmark Harnesses Through Hermes Runtime

## Objective

Create inspectable proof and benchmark harnesses that measure Core2 through the real Hermes runtime path rather than detached model or store shortcuts.

## Must Haves

- Proof/benchmark code runs through Hermes-path wiring.
- The harness is deterministic enough for local proof and structured enough for later larger runs.
- Legacy benchmark direction is reused where it still fits, without reviving obsolete kernel assumptions wholesale.

## Tasks

<task>
<id>04-02-T1</id>
<title>Define a Hermes-path Core2 proof harness for local deterministic verification</title>
<read_first>
- run_agent.py
- agent/memory_manager.py
- plugins/memory/core2/__init__.py
- .planning/legacy-test-seeds/kernel-memory/test_hermes_kernel_e2e_benchmark.py
</read_first>
<action>
Implement a deterministic proof harness that exercises Core2 through Hermes-path runtime/provider behavior and emits inspectable structured results. The harness should prove real storage, retrieval, and answer-path behavior without requiring a paid benchmark run.
</action>
<acceptance_criteria>
- there is a runnable local proof harness for Hermes+Core2
- the harness produces structured output or a report, not only console prose
- the harness touches the real Hermes runtime/provider path
</acceptance_criteria>
</task>

<task>
<id>04-02-T2</id>
<title>Revive a Core2-compatible benchmark/eval slice from legacy seeds</title>
<read_first>
- .planning/legacy-test-seeds/kernel-memory/test_hermes_longmemeval_benchmark.py
- .planning/legacy-test-seeds/kernel-memory/test_replay_benchmarks.py
- .planning/legacy-test-seeds/kernel-memory/test_hermes_kernel_e2e_benchmark.py
</read_first>
<action>
Extract the still-useful benchmark ideas from the legacy kernel tests and adapt one realistic slice to Core2:
- Hermes-path subset verification
- replay/token proof reporting
- benchmark result schema that can compare modes honestly

Do not force obsolete KernelMemory assumptions into Core2.
</action>
<acceptance_criteria>
- at least one legacy benchmark idea is revived in a Core2-compatible way
- the benchmark/eval slice stays Hermes-path, not model-only
- outputs are structured and comparable across modes
</acceptance_criteria>
</task>

<task>
<id>04-02-T3</id>
<title>Document proof tiers and benchmark gates for later scaling</title>
<read_first>
- .planning/PROJECT.md
- .planning/REQUIREMENTS.md
- .planning/phases/03-retrieval-and-answer-contracts/03-VERIFICATION.md
</read_first>
<action>
Define the Phase 4 proof ladder clearly:
- local deterministic proof
- broader local regression proof
- Hermes-path benchmark/eval proof
- environment-blocked or later scaling gates

Make it obvious which gates are satisfied now and which remain later work.
</action>
<acceptance_criteria>
- proof tiers are explicit and inspectable
- benchmark gates do not overclaim beyond what the repo can currently run
- later larger-scale evaluation work has a clear handoff path
</acceptance_criteria>
</task>

## Verification

- Prove the new harness runs through Hermes-path code
- Prove at least one structured Core2 benchmark/eval slice exists
- Prove proof tiers are documented without model-only shortcuts
