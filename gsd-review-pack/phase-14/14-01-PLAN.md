# Plan 14-01: Freeze Selector Boundary And Target Slice

## Goal

Lock the exact selector scope, target shapes, and observability contract before any implementation starts.

## Tasks

- derive the Phase 14 target slice from the already-supported aggregate-temporal tranche rather than reopening excluded shapes
- freeze the selector feature set that is allowed in this phase
- freeze the exact aggregation safety checks allowed in this phase
- write one boundary artifact that lists supported and excluded shapes explicitly
- prepare one machine-readable target-slice manifest for later local proof

## Deliverables

- `14-SELECTOR-BOUNDARY.md`
- `14-TARGET-SLICE-MANIFEST.json`

## Verification

- target shapes are explicit
- excluded shapes are explicit
- selector features are explicit
- no ranking, delivery, bitemporal, or promotion-gate work is smuggled in
